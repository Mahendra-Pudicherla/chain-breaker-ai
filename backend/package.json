{
  "name": "chain-breaker-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "src/server.main.js",
  "scripts": {
    "build": "npx playwright install chromium --with-deps",
    "dev": "node --watch src/server.main.js",
    "start": "node src/server.main.js"
  },
  "dependencies": {
    "dotenv": "^17.4.1",
    "playwright": "^1.59.1"
  }
}
